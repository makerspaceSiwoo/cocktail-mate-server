# Terraform 로컬 상태/비밀값 — 커밋 금지
.terraform/
.terraform.lock.hcl
*.tfstate
*.tfstate.*
crash.log
crash.*.log

# 변수 파일에는 비밀값 포함 (example만 커밋)
*.tfvars
*.tfvars.json
!*.tfvars.example

# API private key
*.pem
