fastapi
uvicorn[standard]
sqlalchemy
psycopg2-binary
pgvector
python-dotenv
pydantic-settings
# 인증(JWT 쿠키) — access/refresh 발급, rate limit, 소셜(카카오) HTTP 호출
python-jose[cryptography]
slowapi
httpx
# ORM 모델 + 마이그레이션 툴 (로컬 패키지, 모노레포)
-e ./packages/db_schema[migrations]
